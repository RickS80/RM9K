import React from 'react';

const Header = (props) => {
       return(
            <div className="header">
            <a>Home</a>
            <a>New booking</a>
            <a>Table view</a>
            </div>
        )
    }

export default Header;